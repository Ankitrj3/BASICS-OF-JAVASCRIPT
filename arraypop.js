//manipulate array with pop()

//we will use pop function to add the element in array

var pop1=[1,23,4];//it will remove the last element of the array
pop1.pop();
console.log(pop1);

//manipulate array with shift()
pop1.shift();// it will remove the element from the first index of the array
console.log(pop1);

