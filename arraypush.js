//manipulate array with push()

//we will use push function to add the element in array

var oname=["ankit","ranjan","robin","rocky","saurabh"];
oname.push(["shubham","rudra"]);//it will add the element at the last
console.log(oname);

oname.unshift(["kk","djs"]);
console.log(oname);//it will add the element from the first index
